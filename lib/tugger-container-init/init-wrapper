#!/bin/bash

# run provisioning
/bin/bash /tugger/lib/tugger-container-init/provision

# run init script from tugger-stack
/bin/bash /tugger/tugger-stack/init

# keep container running forever (or until it is being stopped)
echo
echo "Container ready, you can press ^C now to get back to your local console."
echo
sleep infinity
